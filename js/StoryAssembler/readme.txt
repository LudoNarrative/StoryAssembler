StoryAssembler

StoryAssembler is a generator for interactive choice-based stories designed to flexibly respond to a game state.

TO RUN

See readme.MD in ClimateGame directory. Navigate to:

	http://localhost:8000/StoryAssembler/

